import React from 'react';

const OrderList = (props) => {
    return (
        <div>
            <h2>Order list</h2>
            <h4>Items ordered : {}</h4>
        </div>
    );
};

export default OrderList;